services:
  - type: web
    name: hireai-backend
    runtime: python
    rootDir: resume-agent/backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port 8000
    envVars:
      - key: OPENAI_API_KEY
        sync: false